# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

A Kotlin service template for polling and processing AWS SQS messages, deployed on ECS Fargate via AWS CDK.

- `service/` — Kotlin application (Gradle, JVM 25)
- `infrastructure/` — AWS CDK stack (TypeScript)

Dependency versions are centralized in `.kiro/VERSIONS.md`.
